# Демонстрационные значения. Скопировать в .env и заполнить локально.
# .env не коммитить (см. .gitignore).
DATABASE_URL=postgresql+psycopg2://postgres:postgres@localhost:5432/med_checks
POSTGRES_DB=med_checks
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
